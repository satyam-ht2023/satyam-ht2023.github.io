{
  "name": "affiliate-hygiene-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "npm run build && touch ./out/.nojekyll && gh-pages -d out --dotfiles --nojekyll",
    "ghd": "touch ./out/.nojekyll && gh-pages -d out --dotfiles --nojekyll"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "next": "15.3.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "touch-cli": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
